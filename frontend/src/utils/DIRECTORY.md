# `frontend/src/utils` 目录说明

## 目录定位
该目录用于承载项目的相关代码/数据/配置；详见下方文件与引用关系。

[上级目录](../DIRECTORY.md)

## 本目录文件概览
- `constants.ts`
- `export.ts`
- `index.ts`

## 脚本/模块说明（本目录内）
### `constants.ts`
- 作用/意义: Application Constants API Configuration
- 路径: `frontend/src/utils/constants.ts`

**被谁引用/调用（代码级）**
- `frontend/src/components/layout/Sidebar/Sidebar.tsx`

**引用了谁（内部依赖）**
（无）

### `export.ts`
- 作用/意义: Export Utilities - CSV/JSON 数据导出工具
- 路径: `frontend/src/utils/export.ts`

**被谁引用/调用（代码级）**
（无）

**引用了谁（内部依赖）**
（无）

**引用了谁（外部依赖）**
- `dayjs`

### `index.ts`
- 作用/意义: Utils - Unified Export
- 路径: `frontend/src/utils/index.ts`

**被谁引用/调用（代码级）**
（无）

**引用了谁（内部依赖）**
（无）

## 引用关系（目录级汇总）
**本目录被谁引用（跨目录）**
- `frontend/src/components/layout/Sidebar/Sidebar.tsx`

**本目录引用了谁（跨目录）**
（无）
