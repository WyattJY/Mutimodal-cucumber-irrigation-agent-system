# `frontend/src/components/prediction` 目录说明

## 目录定位
该目录用于承载项目的相关代码/数据/配置；详见下方文件与引用关系。

[上级目录](../DIRECTORY.md)

## 本目录文件概览
- `EnvDataForm.tsx`
- `index.ts`
- `PredictionResult.tsx`

## 脚本/模块说明（本目录内）
### `EnvDataForm.tsx`
- 作用/意义: EnvDataForm - 环境数据输入表单
- 路径: `frontend/src/components/prediction/EnvDataForm.tsx`

**被谁引用/调用（代码级）**
（无）

**引用了谁（内部依赖）**
（无）

**引用了谁（外部依赖）**
- `react`

### `index.ts`
- 作用/意义: Prediction components index
- 路径: `frontend/src/components/prediction/index.ts`

**被谁引用/调用（代码级）**
- `frontend/src/pages/Predict.tsx`

**引用了谁（内部依赖）**
（无）

### `PredictionResult.tsx`
- 作用/意义: PredictionResult - 预测结果展示组件
- 路径: `frontend/src/components/prediction/PredictionResult.tsx`

**被谁引用/调用（代码级）**
（无）

**引用了谁（内部依赖）**
（无）

**引用了谁（外部依赖）**
- `clsx`

## 引用关系（目录级汇总）
**本目录被谁引用（跨目录）**
- `frontend/src/pages/Predict.tsx`

**本目录引用了谁（跨目录）**
（无）
