global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: agriagent-backend
    metrics_path: /metrics
    static_configs:
      - targets:
          - host.docker.internal:8000
        labels:
          runtime: gdrive-local
